---
title: 4.2 네트워크 설정
description: AntBot 온보드 컴퓨터 네트워크 설정
sidebar:
  order: 2
---

import { Steps } from '@astrojs/starlight/components';

AntBot 컴퓨터는 네트워크 구성 도구로 `netplan` 을 사용합니다.

인터페이스 이름은 **`eth0`** 이며, 온보드 컴퓨터의 고정 IP 주소는 **`192.168.6.2`** 입니다.

별도 요청이 없는 한 AntBot에는 공유기가 함께 설치되어 출고되며, Wi-Fi SSID는 **`antbot_xxxx`** 형식으로 표시됩니다. 비밀번호는 별도 공지를 참고하세요.

## 온보드 컴퓨터에 SSH 접속

<Steps>

1. 랩탑을 AntBot의 Wi-Fi(`antbot_xxxx`)에 연결합니다.

   공유기의 내부 네트워크 주소 범위는 `192.168.6` 대로 설정되어 있습니다.

2. 랩탑의 IP 주소를 `192.168.6.x` 대역으로 설정합니다. (`192.168.6.2`, `192.168.6.5` 제외)

   기본적으로 DHCP를 통해 `192.168.6.x` 대역으로 자동 할당됩니다.

   - `192.168.6.2` — 온보드 컴퓨터 (예약)
   - `192.168.6.5` — 3D LiDAR (예약)

3. 아래 명령어를 통해 ssh 접속합니다.

   ```bash
   ssh antbot@192.168.6.2
   ```

</Steps>

## 온보드 컴퓨터 고정 IP 주소 변경

<Steps>

1. `/etc/netplan/interfaces.yaml` 파일 수정 (addresses 항목)

   ```yaml
   network:
     version: 2
     renderer: networkd
     ethernets:
       eth0:
         dhcp4: no
         addresses: [192.168.6.2/24]
         routes:
           - to: default
             via: 192.168.6.1
         nameservers:
           addresses: [8.8.8.8, 8.8.4.4]
   ```

2. 아래 명령어를 통해 네트워크 설정을 반영합니다.

   ```bash
   sudo netplan apply
   ```

3. 아래 명령어를 통해 인터페이스(`eth0`)에 할당된 IP 주소를 확인합니다.

   ```bash
   ip a
   ```

</Steps>
